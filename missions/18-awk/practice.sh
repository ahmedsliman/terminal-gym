#!/bin/bash
# Mission 18 — awk: Interactive Practice
set -uo pipefail
COURSE_ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
_PRACTICE_PATH="${BASH_SOURCE[0]}"
source "${COURSE_ROOT}/lib/course.sh"

init_mission "18" "awk" 1

step "Coming soon"
explain "This practice session is not yet written.
In the meantime, use:
  make mission   N=18   — read the concept brief
  make exercises N=18   — work through the exercises"

mission_complete
